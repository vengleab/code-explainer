{
  "services": {
    "frontend": {
      "root": "frontend/",
      "installCommand": "npm install",
      "buildCommand": "npm run build",
      "outputDirectory": "dist"
    },
    "backend": {
      "root": "backend/",
      "entrypoint": "generate:app",
      "functions": {
        "generate.py": {
          "includeFiles": "fonts/**",
          "maxDuration": 10
        },
        "generate_pandas.py": {
          "includeFiles": "fonts/**",
          "maxDuration": 10
        }
      }
    }
  },
  "rewrites": [
    { "source": "/api/generate-pandas", "destination": { "service": "backend", "entrypoint": "generate_pandas:app" } },
    { "source": "/api/(.*)", "destination": { "service": "backend" } },
    { "source": "/(.*)", "destination": { "service": "frontend" } }
  ]
}
